<?php
/*
	$dbl = new Db();

	$sqlLan = "SELECT a.LangKey, a.LangText, a.UseFor
	FROM t_ui_language a
	INNER JOIN t_ui_language_menu b ON a.MenuId = b.MenuId
	WHERE b.MenuKey = '$menukey'
	AND a.LangCode = '$lan';";
	$aRows = $dbl->query($sqlLan);
	$TEXT = array(); //For Server/PHP Site
	foreach ($aRows as $row) {	
	   $TEXT[$row['LangKey']] = $row['LangText'];
	}*/
?>
